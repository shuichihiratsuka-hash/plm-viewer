/* ══════════════════════════════════════════════════════════════
   このファイルだけが設定を持つ。★ここ以外に値を書かない。

   CLIENT_ID … Google Cloud で作った OAuth クライアントID（ウェブ）。
     ⚠️ **これは秘密ではない。** ページに書くもので、公開されて問題ない
        （鍵として働くのは「承認済みの JavaScript 生成元」の方）。
     ⚠️ **クライアント シークレットは使わない。絶対にここに書かない。**
     空のままなら Drive から読む機能は出さず、理由を画面に出す
     （黙って白くしない）。ファイルを選ぶ方は空でも動く。

   手順は plm-gas の「3Dビューアの下準備」を見る：
     ・User Type は **内部（Internal）**（制限付きスコープの審査が要らなくなる）
     ・承認済みの JavaScript 生成元は **https://shuichihiratsuka-hash.github.io**
       （★ドメインだけ。パスや末尾の / を付けると弾かれる）
     ・スコープは drive.readonly の1つだけ
   ══════════════════════════════════════════════════════════════ */
/* REDIRECT_URI … ★**ページごと Google へ飛んで戻る**方式で使う（ポップアップを使わない）。
     これを空にすると、ポップアップ方式（1回タップ）に落ちる。
   🔴 **入れる前に Google Cloud で登録すること。**
      クライアントID の画面 →「承認済みのリダイレクト URI」に**同じ文字列**を足す。
      ⚠️ 1文字でも違うと Google のエラー画面（`redirect_uri_mismatch`）が出る。
         末尾の `/` まで一致させる。
   ⚠️ 「JavaScript 生成元」（ドメインだけ）とは**別の欄**。両方要る。
   ★なぜこの方式か：`requestAccessToken()` は必ずポップアップを開き、iOS Safari は
     タップと紐づかないポップアップを止める。ページごと飛べばポップアップが要らない。 */
/* SCOPE_WRITE … ★**STEP 取り込み（`#mode=extract`）のときだけ**足す権限。
     ふだんの 3D 表示では求めない（`authScope()` が出し分ける）。
     ⚠️ **空にすると書き出しの口が「未設定」と断る**（黙って落ちない）。

   🔴 **ここは security の判断が要る。本人が決める。**
     | 値 | できること | 危なさ |
     |---|---|---|
     | `drive.file`（既定）| **このページが作ったファイルだけ**触れる | 小さい |
     | `drive` | 利用者の Drive を**全部**読み書きできる | 大きい |

     ⚠️ **`drive.file` で足りるかは未検証**（2026-09-03 時点）。
        Drive は「アプリが作った or 開いたファイルだけ」なので、
        **`out=` で渡された既存フォルダの中に作れない可能性がある**
        （その場合 My Drive に出来る。ビューアはそれを画面に出して、
         手で移すよう案内する ＝ 行き止まりにはしない）。
     ⇒ 本番で1回試して、フォルダへ入らなければ次のどちらかにする：
        ① ここを `https://www.googleapis.com/auth/drive` にする（権限が大きくなる）
        ② Google Picker で書き先を選ばせる（`drive.file` のまま。実装が増える）
     🔴 **①にするなら、その判断を README に書き残すこと**（権限が広がるので）。

   ⚠️ Google Cloud の「スコープ」にも同じものを足すこと（片方だけでは通らない）。 */
/* PICKER_KEY / APP_ID … ★**Google Picker のための2つ**（2026-09-03）。
   🔴 **なぜ Picker が要るか。** `drive.file` は「このページが作った or 開いた
      ファイル」＝**トークンの持ち主ごと**。このページは**利用者自身の OAuth**
      で動くので、**人が Drive に作ったフォルダには書けません。**
      ⚠️ 「ビューアがフォルダを作って、人が1回だけ取り込みフォルダへ移す」は
         **成立しません** —— 作った人しか書けず、2人目は自分のフォルダを作る
         （行き先が人ごとに分かれる）。
   ⇒ **Picker で選んだフォルダは「開いた」に入る**ので `drive.file` のまま書けます。
      ★選ぶのは**利用者ごとに1回**（`localStorage` に覚える）。

   ★どちらも**公開してよい値**（`CLIENT_ID` と同じ。秘密ではない）。
   | 何 | どこで取るか |
   |---|---|
   | `PICKER_KEY` | Google Cloud →「APIとサービス」→ 認証情報 → **APIキー**を作る |
   | `APP_ID` | Google Cloud の**プロジェクト番号**（プロジェクト設定に出ている）|

   ⚠️ **Google Cloud で「Google Picker API」を有効にする**こと（1回だけ）。
   ⚠️ `APP_ID` を渡さないと、選んでも**このページに開かれない**ことがあります。
   ⚠️ **空のままでも 3D 表示は動きます**（効くのは STEP 取り込みの書き出しだけ）。
      ★空のときは画面が「未設定です」と出します（黙って落ちません）。 */
window.VIEWER_CONFIG = {
  CLIENT_ID: '415008889657-1lpo1jinc0pfjom6mc0rfjdg87ihvv4l.apps.googleusercontent.com',
  SCOPE: 'https://www.googleapis.com/auth/drive.readonly',
  SCOPE_WRITE: 'https://www.googleapis.com/auth/drive.file',
  PICKER_KEY: '',                 /* ★未設定なら書き先を選べません（上の説明）*/
  APP_ID: '',                     /* ★Cloud のプロジェクト番号 */
  REDIRECT_URI: 'https://shuichihiratsuka-hash.github.io/plm-viewer/'
};
